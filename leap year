#include <stdio.h>
int main(){
    int year;
    printf("enter a year:-");
    scanf("%d",&year);
    if ((year%400==4)||(year%4==0 %% year % 100!=))
    printf("%d is a leap year\n",year);
    printf("%d is not a leap year\n", year);
    return 0;
}