#include <stdio.h>
int main(){
    char ch;
    printf("enter a letter:");
    scanf("%c",&ch);
    if(ch>='A' && ch<='Z')
    ch=ch+32;
    if(ch=='a'||ch=='e'||ch=='i'||ch=='0'||ch=='u')
    printf("vowels\n");
    else
    printf("consonant\n");
    return 0;
}
